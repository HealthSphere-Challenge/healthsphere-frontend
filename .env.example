# Public runtime configuration; copy to .env only for local overrides.
# VITE_* values are public browser configuration. Never place secrets here.
# Same-site backend API path. A full development URL must still end in /api/v1.
VITE_API_BASE_URL=/api/v1
