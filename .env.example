# Documentation template only: no runtime exists yet.
# VITE_* values are public browser configuration. Never place secrets here.
# Only the backend origin is allowed; choose the local origin during HS-003.
VITE_API_BASE_URL=
